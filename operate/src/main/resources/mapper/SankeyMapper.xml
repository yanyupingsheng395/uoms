<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linksteady.operate.dao.SankeyMapper">

    <select id="getSpuList" resultType="map">
        select source_name,target_name,t_cnt,id from hanhoogrowth_dev.SANKEY_DATA t where s_rn &lt;=9
    </select>

    <select id="getSunIdList">
        select * from (select id from hanhoogrowth_dev.SANKEY_DATA t  start with t.id = #{id} connect by PRIOR t.id = t.parent_id)
        where id != #{id}
    </select>
</mapper>